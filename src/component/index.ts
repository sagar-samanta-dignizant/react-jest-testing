import Button from "./Button";
import InputField from "./InputField";
import Logo from "./Logo";
import Title from "./Title";
import Page from "./Page";
import Sidebar from "./Sidebar";
import Header from "./Header";
import SectionContainer from "./SectionContainer";

//This is an index file that exports multiple components from a React application. Each component is imported and then re-exported to be used in other parts of the application.
export {
  Button,
  InputField,
  Logo,
  Title,
  Page,
  Sidebar,
  Header,
  SectionContainer,
};
