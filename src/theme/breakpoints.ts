//his code exports an object named breakpoints which contains breakpoint values for different screen sizes. The keys of the values object represent the screen sizes and the values represent the minimum width at which that screen size begins. 
const breakpoints = {
  values: {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1536
  }
};

export default breakpoints;
